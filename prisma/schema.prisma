datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Build {
  id        String   @id @default(cuid())
  scenario  String
  html      String
  createdAt DateTime @default(now())
}
